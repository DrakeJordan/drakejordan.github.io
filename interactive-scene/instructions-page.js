function instructions() {
  textAlign(CENTER);
  fill(strokeColor);
  text(
    "INSTRUCTIONS GO HERE, ONCE I DECIDE WHAT THIS GAME EVEN DOES",
    width / 2,
    height / 2
  );
  text(
    "Press \"Backspace\" to return to the start screen.",
    width / 2,
    height - 50
  );
}